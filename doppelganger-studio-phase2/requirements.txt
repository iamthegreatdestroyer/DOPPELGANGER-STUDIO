# doppelganger-studio-phase2 dependencies
#
# RECONSTRUCTED 2026-07-06 -- this file's original content was lost (see
# git history: it was introduced already-corrupted in commit d6f4ab4, no
# prior version exists in any reachable git object). This list is grounded
# in: (1) file paths under src/ implying real integrations (postgres.py,
# mongodb.py, redis_cache.py, claude_client.py, wikipedia/tmdb/imdb
# scrapers), (2) PHASE_2_COMPLETION_VERIFICATION.md's per-module dependency
# notes (a self-reported doc, treated as corroborating evidence only, not
# ground truth), and (3) the root repo's real, working pyproject.toml.
# It has NOT been verified against phase2's actual runtime behavior since
# that source code is itself corrupted with the same wikipedia-scraper
# placeholder content (see repo-wide corruption note in commit history).

# Research scrapers
aiohttp>=3.12.15
beautifulsoup4>=4.14.2
Wikipedia-API>=0.8.1
requests>=2.32.5

# Database layer (Postgres / MongoDB / Redis)
asyncpg>=0.29.0
motor>=3.3.2
redis>=5.0.1

# AI client -- No-OpenAI policy: openai intentionally omitted
# (see root repo commit fb6cad4, 2026-07-02)
anthropic>=0.69.0

# Config / models
pydantic>=2.11.10
python-dotenv>=1.1.1

# Dev / test
pytest>=8.4.2
pytest-asyncio>=1.2.0
pytest-cov>=7.0.0
black>=25.9.0
flake8>=7.3.0
mypy>=1.18.2
